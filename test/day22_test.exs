defmodule AOC2015.Day22Test do
  use ExUnit.Case
  # alias AOC2015.Day22
  # the official examples assume a starting mana of 250,
  # but this implementation and the actual question always starts with 500.
  # since the simulation doesn't support overriding initial mana,
  # reproducing those examples in tests is unfeasible without code changes.
end
